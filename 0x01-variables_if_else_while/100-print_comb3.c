#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	int n;
	int x;

	for(n = '0'; n <= '100'; n++)
	{
		for(x = '2'; x <= '100'; x++)
		{
			if(n < x & n != x)
				if(a <= '98' & <= '99')
					putchar(',');
			putchart(' ');
				}

			}
	putchar('\n');
	return (0);

}
