#include <stdio.h>

/**
 * print_numbers - that prints the numbers.
 *
 * Return: adds twu integers
 */

void print_numbers(void)

{

	int n;

	for (n = 48; n <= 57; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
