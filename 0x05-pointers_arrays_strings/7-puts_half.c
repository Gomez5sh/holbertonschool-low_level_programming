#include "holberton.h"

/**
 * puts_half - Prints every other character of a string
 * @str: The evaluated input
 *
 * Return: n value
 */

void puts_half(char *str)
{
	int n = 0;
	int l;
	int c;

	while (str[n] != '\0')
	{
		n++;
	}
