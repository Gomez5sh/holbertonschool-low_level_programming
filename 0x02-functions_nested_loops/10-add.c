#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @n: parameters input
 * @x: parameters input
 * Return: adds twu integers
 */

int add(int n, int x)

{
	return (n + x);
}
